/*const {suma} = require('./index')

console.log(suma(21,2))

const {usuarios} = require('./usuarios.json')

console.log(usuarios)
*/

export function greet(name) {
    return `Hello, ${name}`
}

modules.exports = {greet}